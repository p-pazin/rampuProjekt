<resources xmlns:tools="http://schemas.android.com/tools">
    <attr name="iconColor" format="color" />
    <style name="Base.Theme.Carchive" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:colorPrimary">@color/primaryColorLight</item>
        <item name="android:colorAccent">@color/accentColorLight</item>
        <item name="android:textColor">@color/textColorLight</item>
        <item name="backgroundColor">@color/primaryColorLight</item>
        <item name="android:fontFamily">@font/inter_regular</item>
        <item name="android:textColorHint">@color/hintTextColor</item>
        <item name="buttonTint">@color/primaryColorLight</item>
        <item name="itemBackground">@color/textColorLight</item>
        <item name="tabTextColor">@color/textColorLight</item>
        <item name="android:statusBarColor">@color/accentColorLight</item>
        <item name="iconColor">@color/iconsColorLight</item>
    </style>

    <style name="CustomBackground">
        <item name="android:background">@color/primaryColorLight</item>
    </style>

    <style name="PopUpBackGround">
        <item name="android:backgroundTint">@color/primaryColorLight</item>
    </style>

    <style name="Theme.Carchive" parent="Base.Theme.Carchive" />

</resources>