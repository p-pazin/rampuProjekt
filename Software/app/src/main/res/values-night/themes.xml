<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Carchive.Night" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:colorPrimary">@color/primaryColorDark</item>
        <item name="android:colorAccent">@color/accentColorDark</item>
        <item name="android:textColor">@color/textColorDark</item>
        <item name="backgroundColor">@color/primaryColorDark</item>
        <item name="android:fontFamily">@font/inter_regular</item>
        <item name="itemBackground">@color/textColorDark</item>
        <item name="backgroundTint">@color/primaryColorDark</item>
        <item name="tabTextColor">@color/textColorDark</item>
        <item name="android:statusBarColor">@color/accentColorLight</item>
    </style>

    <style name="CustomBackground">
        <item name="android:background">@color/accentColorLight</item>
    </style>

    <style name="Theme.Carchive" parent="Base.Theme.Carchive" />
</resources>